# Internal hook to optionally initialize the data directory when the user opts in via \`options(weatherfunctions.auto_setup = TRUE)\`.

Internal hook to optionally initialize the data directory when the user
opts in via \`options(weatherfunctions.auto_setup = TRUE)\`.

## Usage

``` r
.onLoad(...)
```
