# WriteHumeWeatherFile

WriteHumeWeatherFile

## Usage

``` r
WriteHumeWeatherFile(df, fn)
```

## Arguments

- df:

  data frame with weather data

- fn:

  file name for output

## Value

none, a file is written

## Examples

``` r
WriteHumeWeatherFile(df, fn)
#> Error in WriteHumeWeatherFile(df, fn): Not all column names for HUME file in data frame.
```
