# abr.names shorten long column names

abr.names shorten long column names

## Usage

``` r
abr.names(tab)
```

## Arguments

- tab:

  Data frame for which columns should be renamed

## Value

Data frame with shortened column names
